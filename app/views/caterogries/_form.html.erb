<%= form_for(@caterogry) do |f| %>
  <% if @caterogry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caterogry.errors.count, "error") %> prohibited this caterogry from being saved:</h2>

      <ul>
      <% @caterogry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
